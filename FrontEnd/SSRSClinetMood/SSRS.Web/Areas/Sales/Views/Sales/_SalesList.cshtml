@model IEnumerable<SSRS.Web.Areas.Sales.Models.SalesViewModel>

<div class="card card-info">
    <div class="card-header">
        <h3 class="card-title">Sales List</h3>
        <div class="card-tools">
            <button type="button" class="btn btn-tool" data-card-widget="collapse">
                <i class="fas fa-minus"></i>
            </button>
        </div>
    </div>
    <!-- /.card-header -->
    <div class="card-body table-responsive p-0">
        <table id="tblSales" class="table table-bordered table-striped table-hover text-nowrap">
            <thead>
                <tr>
                    <th>Product Name</th>
                    <th>SKU Code</th>
                    <th>Sales Quantity</th>
                    <th>Stock Quantity</th>
                    <th>Price</th>
                    <th>Total Proce</th>
                    <th class="tableButton1">Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.ProductName</td>
                        <td>@item.SKUCode</td>
                        <td>@item.QuantitySold</td>
                        <td>@item.StockQty</td>
                        <td>@item.Price</td>
                        <td>@item.TotalPrice</td>
                        <td>
                            <button class="btn btn-sm btn-info" type="button" onclick="GetProductFunction(@item.Id);"><i class="fas fa-edit"></i></button>
                           
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <!-- /.card-body -->
</div>


